import { PracticeDashboard } from "./practice-dashboard.model";

describe("PracticeDashboard", () => {
  it("should create an instance", () => {
    expect(new PracticeDashboard()).toBeTruthy();
  });
});
