import { ClientProviderMapping } from "../../../Pages/AR Management/Common/Configuration/clientprovidermapping/client-provider-mapping.model";

describe("ClientProviderMapping", () => {
  it("should create an instance", () => {
    expect(new ClientProviderMapping()).toBeTruthy();
  });
});
