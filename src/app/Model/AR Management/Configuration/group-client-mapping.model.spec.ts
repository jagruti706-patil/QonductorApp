import { GroupClientMapping } from './group-client-mapping.model';

describe('GroupClientMapping', () => {
  it('should create an instance', () => {
    expect(new GroupClientMapping()).toBeTruthy();
  });
});
